The install is a pain, but try to do that and do not blame $DEITY:

Deps:

celery
virtuoso
rabbitmq
postgresql with postgis
patched pubby for mapped.example.org and raw.example.org
supervisord
ruby-compass

the repos ../data_acquisition and ../spazio_flow
requirements.txt

create a user for it:

# adduser --group --system --home=/var/lib/controller controller
# install --directory --group=adm --user=controller --mode 2775 /var/log/controller
# adduser controller virtuoso

also a db user:
# createuser controller
# createdb controller -O controller

generate a virtuoso data input directory to deploy RDF generated by the controller workflow

# install --directory --group=virtuoso --user=virtuoso --mode=2775 /srv/virtuoso/data

Add /srv/virtuoso/'DirsAllowed' in virtuoso config

./manage.py collectstatic --noinput

# log as controller and generate a virtualenv
$ .  /etc/bash_completion.d/virtualenvwrapper
$ workon controller

copy settings_local and modify username/password

# cp conf/settings_local.py /etc/controller_py.conf
$ ln -s /etc/controller_py.conf /var/lib/controller/.virtualenvs/controller/lib/python2.7/site-packages/settings_local.py


copy conf/supervisord/controller.conf in /etc/supervisor/conf.d/
copy conf/nginx/controller.example.org /etc/nginx/conf.d/







